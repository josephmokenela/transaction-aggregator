apiVersion: v1
kind: Service
metadata:
  name: transaction-aggregator
  namespace: transaction-aggregator
spec:
  selector:
    app: transaction-aggregator
  ports:
    - port: 8080
      targetPort: 8080
  type: ClusterIP
