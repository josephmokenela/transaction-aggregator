apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: transaction-aggregator
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
  # ClusterIP — only reachable inside the cluster (no need to expose externally)
  type: ClusterIP
