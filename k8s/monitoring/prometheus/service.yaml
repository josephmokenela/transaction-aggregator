apiVersion: v1
kind: Service
metadata:
  name: prometheus
  namespace: transaction-aggregator
spec:
  selector:
    app: prometheus
  ports:
    - port: 9090
      targetPort: 9090
      nodePort: 30900
  type: NodePort
