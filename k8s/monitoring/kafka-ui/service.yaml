apiVersion: v1
kind: Service
metadata:
  name: kafka-ui
  namespace: transaction-aggregator
spec:
  selector:
    app: kafka-ui
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30890
  type: NodePort
