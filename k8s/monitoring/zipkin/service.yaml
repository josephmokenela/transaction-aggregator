apiVersion: v1
kind: Service
metadata:
  name: zipkin
  namespace: transaction-aggregator
spec:
  selector:
    app: zipkin
  ports:
    - port: 9411
      targetPort: 9411
      nodePort: 30411
  type: NodePort
